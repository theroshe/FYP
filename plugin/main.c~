#include <stdio.h>

int main()
{
  struct test {  	  
    int i : 1;	
    int j : 2;	
  } __attribute__((bit_band(5)));	
  
  //struct test_t test __attribute__((bit_band(5)));
  int i __attribute__((bit_band()));
  
  return 0;
}
